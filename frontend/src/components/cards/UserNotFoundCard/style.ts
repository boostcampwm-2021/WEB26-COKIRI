import styled from '@emotion/styled';

const Wrapper = styled.div`
  font-size: 24px;

  div {
    display: flex;
    justify-content: center;
    align-items: center;
  }
`;

const Bold = styled.b`
  margin-right: 8px;
`;

export { Wrapper, Bold };
