import styled from '@emotion/styled';

const Wrapper = styled.div`
  padding: 50px 0;
`;
const Row = styled.div`
  display: flex;
  align-items: center;
  margin-left: 10px;
  height: 64px;
  div {
    margin: 0 5px;
  }
`;

export { Wrapper, Row };
