import styled from '@emotion/styled';

const Wrapper = styled.div`
  position: absolute;
  z-index: 3;
`;

const Repos = styled.div`
  height: 400px;
  width: 350px;
  overflow-y: scroll;
`;

export { Wrapper, Repos };
