import styled from '@emotion/styled';

const Wrapper = styled.div`
  position: absolute;
  z-index: 3;
`;

const Repos = styled.div`
  overflow-y: auto;
  height: 400px;
`;

export { Wrapper, Repos };
